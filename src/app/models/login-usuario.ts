export class LoginUsuario {
    numCompte: string;
    password: string;
    constructor(numCompte: string, password: string) {
        this.numCompte = numCompte;
        this.password = password;
    }
}
